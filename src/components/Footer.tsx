import  styles from "./Footer.module.css"

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <p>
        <span>To-Do React</span> @2024
      </p>
    </footer>
  );
};

export default Footer;
